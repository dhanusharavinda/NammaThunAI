# Frontend (React + Vite)

## Configure

Copy `.env.example` to `.env` and set:

- `VITE_BACKEND_URL` (default `http://127.0.0.1:8000`)

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173`.

